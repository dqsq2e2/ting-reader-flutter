import 'fnos_ws_transport_base.dart';

Future<FnosWebSocketSession> connectFnosWebSocket(
  Uri uri, {
  required Map<String, String> headers,
}) {
  throw UnsupportedError('FNID account login is unavailable on this platform');
}
